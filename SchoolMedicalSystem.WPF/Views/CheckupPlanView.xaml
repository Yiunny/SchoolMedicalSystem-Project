<UserControl x:Class="SchoolMedicalSystem.WPF.Views.CheckupPlanView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             mc:Ignorable="d" 
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="QUẢN LÝ KẾ HOẠCH KHÁM" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>
        <DataGrid Grid.Row="1" ItemsSource="{Binding Plans}" SelectedItem="{Binding SelectedPlan}" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Tên Kế hoạch" Binding="{Binding PlanName}" Width="*"/>
                <DataGridTextColumn Header="Lớp" Binding="{Binding ClassName}"/>
                <DataGridTextColumn Header="Ngày khám" Binding="{Binding CheckupDate, StringFormat='dd/MM/yyyy'}"/>
                <DataGridTextColumn Header="Trạng thái" Binding="{Binding Status}"/>
            </DataGrid.Columns>
        </DataGrid>
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,10,0,0">
            <Button Content="Tạo Kế hoạch" Width="120" Margin="0,0,10,0" Command="{Binding AddPlanCommand}"/>
            <Button Content="Sửa Kế hoạch" Width="120" Command="{Binding EditPlanCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>