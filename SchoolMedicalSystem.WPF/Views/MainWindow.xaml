<Window x:Class="SchoolMedicalSystem.WPF.Views.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:models="clr-namespace:SchoolMedicalSystem.Core.Models;assembly=SchoolMedicalSystem.Core"
    Title="Hệ Thống Y Tế Học Đường" Height="600" Width="900"
    WindowStartupLocation="CenterScreen">

    <Window.Resources>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Background="#F0F0F0">
            <TextBlock Text="CHỨC NĂNG" FontWeight="Bold" Margin="10" FontSize="16"/>

            <StackPanel>
                <StackPanel.Style>
                    <Style TargetType="StackPanel">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding CurrentUserRole}" Value="{x:Static models:UserRole.Manager}">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </StackPanel.Style>
                <Button Content="Quản lý Học sinh" Margin="10,5" Command="{Binding ShowStudentManagementCommand}"/>
                <Button Content="Quản lý Tài khoản" Margin="10,5" Command="{Binding ShowAccountManagementCommand}"/>
                <Button Content="Lập Kế hoạch Khám" Margin="10,5" Command="{Binding ShowCheckupPlanCommand}"/>
                <Button Content="Báo cáo" Margin="10,5"/>
            </StackPanel>

            <StackPanel>
                <StackPanel.Style>
                    <Style TargetType="StackPanel">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding CurrentUserRole}" Value="{x:Static models:UserRole.Nurse}">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </StackPanel.Style>
                <Button Content="Kế hoạch Khám" Margin="10,5" Command="{Binding ShowNurseDashboardCommand}"/>
                <Button Content="Tra cứu Học sinh" Margin="10,5" Command="{Binding ShowStudentLookupCommand}"/>

            </StackPanel>

            <Button Content="Đăng xuất" VerticalAlignment="Bottom" Margin="10"/>

        </StackPanel>

        <Border Grid.Column="1" BorderBrush="LightGray" BorderThickness="1,0,0,0">
            <ContentControl x:Name="MainContent"
                            Content="{Binding CurrentView}"/>
        </Border>
    </Grid>
</Window>