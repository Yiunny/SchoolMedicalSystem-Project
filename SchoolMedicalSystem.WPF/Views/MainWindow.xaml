<Window x:Class="SchoolMedicalSystem.WPF.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:models="clr-namespace:SchoolMedicalSystem.Core.Models;assembly=SchoolMedicalSystem.Core"
        mc:Ignorable="d"
        Title="Hệ Thống Y Tế Học Đường" Height="720" Width="1280"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">
    <Window.Resources>
        <Style x:Key="NavButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#444444"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}" CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#EAEAEA"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="White" BorderBrush="#E0E0E0" BorderThickness="0,0,1,0">
            <DockPanel>
                <Button DockPanel.Dock="Bottom" Content="Đăng xuất" Margin="5" Style="{StaticResource NavButtonStyle}" Command="{Binding LogoutCommand}"/>

                <StackPanel DockPanel.Dock="Top">
                    <TextBlock Text="Hệ Thống Y Tế" FontSize="18" FontWeight="Bold" Margin="20" HorizontalAlignment="Center" Foreground="#007ACC"/>
                    <Separator/>

                    <StackPanel Visibility="{Binding CurrentUserRole, ConverterParameter={x:Static models:UserRole.Manager}, Converter={StaticResource RoleToVisibilityConverter}}">
                        <Button Content="Trang chủ" Margin="5" Style="{StaticResource NavButtonStyle}" Command="{Binding ShowHomePageCommand}"/>

                        <Button Content="Quản lý HỌC SINH" 
                            Margin="5" 
                                Style="{StaticResource NavButtonStyle}"
                            Command="{Binding ShowStudentManagementCommand}"
                            />

                        <Button Content="Quản lý Tài khoản" Margin="5" Style="{StaticResource NavButtonStyle}" Command="{Binding ShowAccountManagementCommand}"/>
                        <Button Content="Lập Kế hoạch Khám" Margin="5" Style="{StaticResource NavButtonStyle}" Command="{Binding ShowCheckupPlanCommand}"/>
                        <Button Content="Báo cáo" Margin="5" Style="{StaticResource NavButtonStyle}" Command="{Binding ShowDashboardCommand}"/>

                        
                    </StackPanel>

                    <StackPanel Visibility="{Binding CurrentUserRole, ConverterParameter={x:Static models:UserRole.Nurse}, Converter={StaticResource RoleToVisibilityConverter}}">
                        <Button Content="Trang chủ" Margin="5" Style="{StaticResource NavButtonStyle}" Command="{Binding ShowHomePageCommand}"/>
                        <Button Content="Kế hoạch Khám" Margin="5" Style="{StaticResource NavButtonStyle}" Command="{Binding ShowNurseDashboardCommand}"/>
                        <Button Content="Tra cứu Học sinh" Margin="5" Style="{StaticResource NavButtonStyle}" Command="{Binding ShowStudentLookupCommand}"/>
                    </StackPanel>
                </StackPanel>
            </DockPanel>
        </Border>

        <ContentControl Grid.Column="1" Content="{Binding CurrentView}"/>
    </Grid>
</Window>